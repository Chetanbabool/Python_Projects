# Random Password generator 

import random
import string
def password_generator(pass_length):
    empty_pass = ""
    chart_value = string.ascii_letters + string.digits + string.punctuation
    for i in range (pass_length):
        empty_pass += random.choice(chart_value)
    
    return empty_pass

print(password_generator(pass_length)
